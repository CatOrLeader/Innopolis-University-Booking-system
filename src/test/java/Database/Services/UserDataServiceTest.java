package Database.Services;

import Database.DbConnector;
import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.assertFalse;


public class UserDataServiceTest {

    // TODO: Configure proper Integration tests with automated DB setup and shutdown

    @Test
    public void userExistsTest() {
//        DbConnector connection = new DbConnector();
//        UserDataService userDataService = new UserDataService(connection.getConnection());
//        assertFalse(userDataService.userExists(90000000));
    }

    @Test
    public void isAuthorizedTest() {
//        DbConnector connection = new DbConnector();
//        UserDataService userDataService = new UserDataService(connection.getConnection());
//        assertFalse(userDataService.isAuthorized(580245280));
    }
}